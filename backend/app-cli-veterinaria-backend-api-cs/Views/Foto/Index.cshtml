@model IEnumerable<Project.Models.Foto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrImagen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntIdMascota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrImagen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntIdMascota)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntIdFoto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntIdFoto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntIdFoto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
